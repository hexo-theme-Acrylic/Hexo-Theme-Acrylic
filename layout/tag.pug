extends includes/layout.pug

block content
  include ./includes/mixins/article-sort.pug
  #tag
    #tag-page-tags
      !=pagecloud({source: site.tags})
    .article-sort-title= _p('page.tag')
    +articleSort(page.posts)
    include includes/part/pagination.pug